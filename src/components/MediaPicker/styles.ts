import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        marginTop: 8,
    },
    image: {
        width: 64,
        height: 64,
        marginRight: 8,
        borderRadius: 8,
    },
});
