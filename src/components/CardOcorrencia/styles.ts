import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        borderWidth: 1,
        borderColor: "#d1d1d1",
        borderRadius: 10,
        padding: 12,
        marginBottom: 12,
    },
    title: {
        fontWeight: "700",
    },
    info: {
        color: "#777777",
    },
    address: {
        color: "#777777",
        marginTop: 4,
    },
    actions: {
        alignItems: "flex-end",
        marginTop: 8,
    },
});
