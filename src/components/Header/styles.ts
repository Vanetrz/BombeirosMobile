import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#007bff",
        padding: 16,
    },
    title: {
        color: "#fff",
        fontWeight: "700",
        fontSize: 18,
    },
    subtitle: {
        color: "#fff",
        opacity: 0.85,
    },
});