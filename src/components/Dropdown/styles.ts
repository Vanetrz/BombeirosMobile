import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
    },
    group: {
        width: '100%',
        height: 48,
        paddingHorizontal: 8,
        justifyContent: 'center',

        borderWidth: 1,
        borderColor: '#D7DEDD',
        borderRadius: 8,
    },
    label: {
        marginBottom: 4,
        fontWeight: '500',
    },
    picker: {
        width: '100%',
        borderRadius: 8,
    },
});